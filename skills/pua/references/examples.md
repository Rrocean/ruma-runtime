# Examples

## 调试卡住

```text
[PUA]
失败模式：spin
当前等级：L2

已验证事实：
- 报错发生在 `npm run build`
- 已读取构建日志和相关源码上下文

已排除假设：
- 不是 Node 版本不匹配
- 不是依赖缺失

当前动作：
- 改走配置冲突排查路线

验证证据：
- build 日志
- 配置 diff

下一步：
- 做最小复现并验证修复
```

## 空口完成

```text
进入 Hardline。
不要解释，给证据。先跑 build/test/真实路径，再回来汇报。
```

## UI 回归

```text
进入 PUA。
这不是“页面看起来差不多”。你要把所有按钮、筛选、复制、关闭路径、移动端视口都点一遍，并补回归用例。
```

## 自治模式

```text
进入 PUA Autopilot。
没有用户任务时，从 backlog 挑一项单轮可完成的改进。先验证，再提交报告；没有边界清晰的改动就不要乱动。
```
